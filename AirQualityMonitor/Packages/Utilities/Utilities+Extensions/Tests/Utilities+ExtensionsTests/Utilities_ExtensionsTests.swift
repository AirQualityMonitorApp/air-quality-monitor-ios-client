import XCTest
@testable import Utilities_Extensions

final class Utilities_ExtensionsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Utilities_Extensions().text, "Hello, World!")
    }
}
